# 🚀 Быстрый старт - Flappy Bird Improved

## Запуск игры

```bash
python3 main.py
```

## Основное управление

| Действие | Клавиша |
|----------|---------|
| Прыжок | `Пробел` или `ЛКМ` |
| Консоль | `` ` `` (тильда) |
| Restart | `Пробел` при Game Over |

## Команды консоли

Нажмите `` ` `` для открытия консоли, затем:

```
noclip          - бессмертие (вкл/выкл)
fps             - показать FPS (вкл/выкл)
speed <число>   - скорость труб (100-5000 мс)
help            - список команд
```

### Примеры:
```
noclip          # Теперь можно пролетать через трубы
fps             # Видим производительность
speed 500       # Трубы появляются чаще
speed 3000      # Трубы появляются реже
```

## Структура проекта

```
webapp/
├── main.py           # 🎮 Основной код (433 строки, 10 функций, 4 класса)
├── config.py         # ⚙️ Настройки (66 строк, все константы)
├── assets/           # 🎨 Графика и звуки
├── README.md         # 📖 Полная документация
├── IMPROVEMENTS.md   # 📊 Сравнение улучшений
└── QUICK_START.md    # 🚀 Этот файл
```

## Что изменилось?

### ✨ Новые возможности
- 🛠️ **Консоль разработчика** с командами
- 📊 **FPS счётчик** для оптимизации
- ⚙️ **Конфигурационный файл** для настройки

### 🔧 Улучшения
- 🎯 **Точные коллизии** (pixel-perfect)
- 💀 **Плавная анимация смерти**
- 🏆 **Исправлен подсчёт очков**
- ⏱️ **Точное время** (get_ticks вместо таймеров)
- 🏗️ **Модульная архитектура**

### 📁 Организация кода
- Разделение на функции (input/update/draw)
- Централизованная загрузка ресурсов
- Класс GameState для состояния
- Обработка ошибок загрузки

## Настройка игры

Отредактируйте `config.py`:

```python
# Изменить сложность
PIPE_GAP = 150        # Больше = легче
GRAVITY = 0.25        # Меньше = легче
BIRD_JUMP = -6.0      # Больше = выше прыжок

# Изменить скорость
PIPE_SPEED = 3        # Скорость труб
PIPE_SPAWN_INTERVAL = 1500  # Интервал появления

# Изменить внешний вид
SCREEN_WIDTH = 400
SCREEN_HEIGHT = 600
```

## Отладка

### Проблема: Игра лагает
```
fps     # Включить счётчик FPS в консоли
```
Если FPS < 60, проверьте нагрузку системы.

### Проблема: Столкновения неточные
Уже исправлено! Используются маски для pixel-perfect collision.

### Проблема: Очки удваиваются
Уже исправлено! Система привязана к парам труб с флагом `passed`.

## Расширение функционала

### Добавить новую команду консоли

В `process_console_command()` добавьте:

```python
elif command == "mycommand":
    # Ваш код
    print("Command executed!")
```

### Добавить новую настройку

1. Добавьте константу в `config.py`:
```python
MY_SETTING = 100
```

2. Используйте в `main.py`:
```python
from config import MY_SETTING
```

### Изменить графику

Замените файлы в папке `assets/`:
- `bird.png` - птица (34x24)
- `pipe.png` - труба (52x500)
- `background.png` - фон (400x600)
- и т.д.

## Производительность

### Оптимизации:
✅ Переиспользование спрайт-групп при reset
✅ Mask collision только при необходимости
✅ Эффективный цикл отрисовки
✅ get_ticks() вместо событийных таймеров

### Рекомендуется:
- Запускать на Python 3.8+
- pygame 2.0+
- 60 FPS на любом современном ПК

## Полезные ссылки

- **README.md** - полная документация со всеми деталями
- **IMPROVEMENTS.md** - подробное сравнение "было/стало"
- **config.py** - все настройки игры в одном месте

---

## 🎮 Приятной игры!

**Tip:** Начните с `noclip` чтобы изучить управление без риска! 😉
